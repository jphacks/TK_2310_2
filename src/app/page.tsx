// import { AppBar } from '@mui/material';

import { Typography } from '@mui/material';

const HomePage = () => {
  return (
    <main>
      {/* <AppBar></AppBar> */}
      <Typography variant='h1'>SAFA</Typography>
    </main>
  );
};

export default HomePage;
